{
  "name": "circom-template",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "pnpm protocol:build && pnpm frontend:build",
    "test": "pnpm protocol:test && pnpm frontend:test",
    "dev": "pnpm -r --parallel exec -- pnpm run dev",
    "protocol:build": "pnpm -r --filter 'protocol...' exec -- pnpm run build",
    "protocol:test": "pnpm -r --filter 'protocol' test",
    "protocol:dev": "pnpm -r --parallel --filter 'protocol...' exec -- pnpm run dev",
    "frontend:dev": "pnpm --filter 'frontend' dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engine": {
    "node": ">=18"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "wait-on": "^7.2.0"
  },
  "resolutions": {
    "circom_runtime": "0.1.25"
  }
}
